name: Trigger auto deployment for container-app-qkqldiou3y57m

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/container-app-qkqldiou3y57m-AutoDeployTrigger-6d58bf30-9d9e-48fe-95cf-39a4f737f928.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPQKQLDIOU3Y57M_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPQKQLDIOU3Y57M_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPQKQLDIOU3Y57M_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryqkqldiou3y57m.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPQKQLDIOU3Y57M_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPQKQLDIOU3Y57M_REGISTRY_PASSWORD }}
          containerAppName: container-app-qkqldiou3y57m
          resourceGroup: githubactiontestapppython99ef
          imageToBuild: containerregistryqkqldiou3y57m.azurecr.io/container-app-qkqldiou3y57m:${{ github.sha }}
          


